<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= teacher ? 'Редагувати вчителя' : 'Додати вчителя' %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1><%= teacher ? 'Редагувати вчителя' : 'Додати вчителя' %></h1>
        
        <% if (error) { %>
            <div class="error"><%= error %></div>
        <% } %>
        
        <form action="<%= teacher ? '/admin/teachers/edit/' + teacher.id : '/admin/teachers/add' %>" method="POST" class="teacher-form">
            <div class="form-group">
                <label for="full_name">Повне ім'я:</label>
                <input type="text" id="full_name" name="full_name" value="<%= teacher ? teacher.full_name : '' %>" required>
            </div>
            
            <div class="form-group">
                <label for="subject">Предмет:</label>
                <input type="text" id="subject" name="subject" value="<%= teacher ? teacher.subject : '' %>" required>
            </div>
            
            <div class="form-group">
                <label for="description">Опис:</label>
                <textarea id="description" name="description" rows="4"><%= teacher ? teacher.description : '' %></textarea>
            </div>
            
            <div class="form-actions">
                <button type="submit" class="btn"><%= teacher ? 'Оновити' : 'Додати' %></button>
                <a href="/admin/teachers" class="btn btn-secondary">Скасувати</a>
            </div>
        </form>
        
        <div class="navigation">
            <a href="/admin/teachers" class="btn">Назад до списку вчителів</a>
            <a href="/admin" class="btn">Адмін-панель</a>
            <a href="/dashboard" class="btn">Мій профіль</a>
            <a href="/logout" class="btn logout-btn">Вийти</a>
        </div>
    </div>
</body>
</html>